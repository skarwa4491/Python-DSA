from tracemalloc import start


class Solution:
    def reverseStr(self, s: str, k: int) -> str:
        start_limit = 0
        for i in range(k-1,len(s),k):
            print(s[start_limit:i+1][::-1])
            start_limit=i
            
Solution().reverseStr("abcdefg",2)